import fs from "node:fs";

const appPath = new URL("../src/App.tsx", import.meta.url);
let code = fs.readFileSync(appPath, "utf8");

if (!code.includes('import "./payment.css";')) {
  code = code.replace('import "./catalog.css";', 'import "./catalog.css";\nimport "./payment.css";');
}

if (!code.includes('const [paymentMethod, setPaymentMethod]')) {
  code = code.replace(
    '  const [cart, setCart] = useState<Product[]>([]);',
    '  const [cart, setCart] = useState<Product[]>([]);\n  const [paymentMethod, setPaymentMethod] = useState("Carte bancaire");\n  const [cardName, setCardName] = useState("");\n  const [cardNumber, setCardNumber] = useState("");\n  const [cardExpiry, setCardExpiry] = useState("");\n  const [cardCvv, setCardCvv] = useState("");'
  );
}

if (!code.includes('const removeAllOfProduct')) {
  const marker = `  /* =========================\n     CLIENT LOGIN PAGE\n  ========================= */`;
  const idx = code.indexOf(marker);
  if (idx !== -1) {
    const helpers = `  const removeAllOfProduct = (id: number) => {\n    setCart((current) => current.filter((item) => item.id !== id));\n  };\n\n  const increaseCartProduct = (product: Product) => {\n    if (cart.filter((item) => item.id === product.id).length >= product.stock) return;\n    setCart((current) => [...current, product]);\n  };\n\n  const decreaseCartProduct = (id: number) => {\n    setCart((current) => {\n      const index = current.findIndex((item) => item.id === id);\n      if (index === -1) return current;\n      const copy = [...current];\n      copy.splice(index, 1);\n      return copy;\n    });\n  };\n\n  const groupedCart = cart.reduce<{ product: Product; quantity: number }[]>((groups, product) => {\n    const existing = groups.find((group) => group.product.id === product.id);\n    if (existing) existing.quantity += 1;\n    else groups.push({ product, quantity: 1 });\n    return groups;\n  }, []);\n\n  const cartSubtotal = cart.reduce((sum, product) => sum + product.price, 0);\n  const deliveryFee = cartSubtotal >= 300 || cartSubtotal === 0 ? 0 : 30;\n  const checkoutTotal = cartSubtotal + deliveryFee;\n\n`;
    code = code.slice(0, idx) + helpers + code.slice(idx);
  }
}

if (!code.includes('className="checkout-page"')) {
  const marker = `  /* =========================\n     CLIENT SHOP\n  ========================= */`;
  const idx = code.indexOf(marker);
  if (idx !== -1) {
    const checkoutPages = `  if ((page === "checkout" || page === "payment" || page === "confirmation") && clientLoggedIn) {\n    if (page === "confirmation") {\n      return (\n        <div className="checkout-page">\n          <header className="checkout-header">\n            <div className="checkout-brand"><strong>OSRAH</strong><span>Cosmétiques</span></div>\n            <div className="secure-payment">🔒 Paiement fiable</div>\n          </header>\n          <div className="checkout-steps"><span>Panier</span><b>›</b><span>Validation</span><b>›</b><span>Payer</span><b>›</b><strong>Confirmation</strong></div>\n          <main className="confirmation-wrap">\n            <div className="confirmation-card">\n              <div className="confirmation-icon">✓</div>\n              <h1>Commande confirmée !</h1>\n              <p>Votre commande OSRAH a bien été enregistrée.</p>\n              <div className="confirmation-number">CMD-{Date.now().toString().slice(-6)}</div>\n              <button onClick={() => { setCart([]); setPage("client-shop"); }}>RETOURNER À LA BOUTIQUE</button>\n            </div>\n          </main>\n        </div>\n      );\n    }\n\n    if (page === "payment") {\n      return (\n        <div className="checkout-page">\n          <header className="checkout-header">\n            <button className="checkout-back" onClick={() => setPage("checkout")}>←</button>\n            <div className="checkout-brand"><strong>OSRAH</strong><span>Cosmétiques</span></div>\n            <div className="secure-payment">🔒 Paiement fiable</div>\n          </header>\n          <div className="checkout-steps"><span>Panier</span><b>›</b><span>Validation</span><b>›</b><strong>Payer</strong><b>›</b><span>Confirmation</span></div>\n          <main className="payment-layout">\n            <section className="payment-panel">\n              <h1>Choisissez votre moyen de paiement</h1>\n              <div className="payment-methods">\n                {["Carte bancaire", "PayPal", "Paiement à la livraison"].map((method) => (\n                  <button key={method} className={paymentMethod === method ? "active" : ""} onClick={() => setPaymentMethod(method)}>\n                    <span>{method === "Carte bancaire" ? "💳" : method === "PayPal" ? "P" : "🚚"}</span>{method}\n                  </button>\n                ))}\n              </div>\n\n              {paymentMethod === "Carte bancaire" && (\n                <div className="card-form">\n                  <label>Nom sur la carte</label>\n                  <input value={cardName} onChange={(e) => setCardName(e.target.value)} placeholder="Nom complet" />\n                  <label>Numéro de carte</label>\n                  <input value={cardNumber} onChange={(e) => setCardNumber(e.target.value.replace(/[^0-9 ]/g, "").slice(0, 19))} placeholder="0000 0000 0000 0000" />\n                  <div className="card-row">\n                    <div><label>Date d'expiration</label><input value={cardExpiry} onChange={(e) => setCardExpiry(e.target.value.slice(0, 5))} placeholder="MM/AA" /></div>\n                    <div><label>CVV</label><input value={cardCvv} onChange={(e) => setCardCvv(e.target.value.replace(/\\D/g, "").slice(0, 3))} placeholder="123" /></div>\n                  </div>\n                </div>\n              )}\n\n              <button className="pay-now" onClick={() => {\n                if (paymentMethod === "Carte bancaire" && (!cardName || cardNumber.replace(/\\s/g, "").length < 12 || !cardExpiry || cardCvv.length < 3)) {\n                  alert("Veuillez compléter les informations de paiement.");\n                  return;\n                }\n                setPage("confirmation");\n              }}>PAYER {checkoutTotal.toFixed(2)} DH</button>\n              <p className="demo-note">Simulation de paiement pour la soutenance — aucune transaction réelle n'est effectuée.</p>\n            </section>\n\n            <aside className="checkout-summary payment-summary">\n              <h2>Résumé de votre commande</h2>\n              {groupedCart.map(({ product, quantity }) => (\n                <div className="summary-mini" key={product.id}><img src={product.image} alt={product.name} /><div><strong>{product.name}</strong><span>Qté: {quantity}</span></div><b>{(product.price * quantity).toFixed(2)} DH</b></div>\n              ))}\n              <div className="summary-line"><span>Sous-total</span><strong>{cartSubtotal.toFixed(2)} DH</strong></div>\n              <div className="summary-line"><span>Livraison</span><strong>{deliveryFee === 0 ? "Gratuite" : deliveryFee.toFixed(2) + " DH"}</strong></div>\n              <div className="summary-total"><span>Total</span><strong>{checkoutTotal.toFixed(2)} DH</strong></div>\n            </aside>\n          </main>\n        </div>\n      );\n    }\n\n    return (\n      <div className="checkout-page">\n        <header className="checkout-header">\n          <button className="checkout-back" onClick={() => setPage("client-shop")}>←</button>\n          <div className="checkout-brand"><strong>OSRAH</strong><span>Cosmétiques</span></div>\n          <div className="secure-payment">🔒 Paiement fiable</div>\n        </header>\n        <div className="checkout-steps"><strong>Panier</strong><b>›</b><span>Validation</span><b>›</b><span>Payer</span><b>›</b><span>Confirmation</span></div>\n        <main className="checkout-layout">\n          <section className="checkout-items-panel">\n            <div className="all-items-title"><span>✓</span><h1>TOUS LES ARTICLES ({cart.length})</h1><em>Prix TTC</em></div>\n            <div className="osrah-store-title">♡ OSRAH COSMÉTIQUES</div>\n            <div className="delivery-banner">🚚 Livraison gratuite dès 300 DH <strong>{cartSubtotal < 300 ? "— encore " + (300 - cartSubtotal).toFixed(2) + " DH" : "— offre débloquée !"}</strong></div>\n            {groupedCart.map(({ product, quantity }) => (\n              <article className="checkout-item" key={product.id}>\n                <div className="item-check">✓</div>\n                <img src={product.image} alt={product.name} />\n                <div className="checkout-item-info"><small>{product.category}</small><h3>{product.name}</h3><p>{product.description}</p><strong>{product.price.toFixed(2)} DH</strong></div>\n                <div className="qty-control"><button onClick={() => decreaseCartProduct(product.id)}>−</button><span>{quantity}</span><button onClick={() => increaseCartProduct(product)}>+</button></div>\n                <button className="remove-checkout-item" onClick={() => removeAllOfProduct(product.id)}>🗑</button>\n              </article>\n            ))}\n          </section>\n\n          <aside className="checkout-summary">\n            <h2>Résumé de votre commande</h2>\n            <div className="summary-line"><span>Sous-total</span><strong>{cartSubtotal.toFixed(2)} DH</strong></div>\n            <div className="summary-line"><span>Livraison</span><strong>{deliveryFee === 0 ? "Gratuite" : deliveryFee.toFixed(2) + " DH"}</strong></div>\n            <div className="summary-total"><span>Total TTC</span><strong>{checkoutTotal.toFixed(2)} DH</strong></div>\n            <button disabled={cart.length === 0} onClick={() => setPage("payment")}>PASSER AU PAIEMENT ({cart.length})</button>\n            <p>Vous pourrez choisir votre moyen de paiement à l'étape suivante.</p>\n            <div className="accepted-payments"><h3>NOUS ACCEPTONS</h3><div><span>VISA</span><span>Mastercard</span><span>PayPal</span><span>Apple Pay</span></div></div>\n          </aside>\n        </main>\n      </div>\n    );\n  }\n\n`;
    code = code.slice(0, idx) + checkoutPages + code.slice(idx);
  }
}

code = code.replace(
  /<button\s+onClick=\{\(\) =>\s*alert\(\s*"Votre commande a été enregistrée !"\s*\)\s*\}\s*>\s*PASSER LA COMMANDE\s*<\/button>/,
  '<button onClick={() => setPage("checkout")}>PASSER AU PAIEMENT</button>'
);

fs.writeFileSync(appPath, code, "utf8");
console.log("Demo checkout and payment flow applied");
